import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-falling',
  template: '',
  styleUrl: './falling.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class Falling {}
